<!-- Plugin Configuration File. Read more: https://plugins.jetbrains.com/docs/intellij/plugin-configuration-file.html -->
<idea-plugin>
    <id>dev.gaphunter.railsmassassignmentcompanion</id>
    <name>Rails Mass Assignment Companion</name>
    <vendor>Gap Hunter Labs</vendor>
    <description><![CDATA[
        <p>Warning on <code>params.permit!</code> (or
        <code>xxx_params.permit!</code>) -- Rails' own official
        documentation states: "Extreme care should be taken when using
        permit!, as it will allow all current and future model
        attributes to be mass-assigned". This is the exact anti-pattern
        strong parameters exist to prevent: it disables the whitelist
        entirely, permitting whatever attributes an attacker sends in
        the request body, including ones the developer never intended
        to be settable (the well-known real-world case: a
        <code>role</code>/<code>admin</code> boolean flipped via mass
        assignment).</p>
        <p>Confirmed real gap: none of RuboCop's 7 <code>Security</code>
        cops (<code>Eval</code>, <code>Open</code>,
        <code>IoMethods</code>, <code>MarshalLoad</code>,
        <code>YAMLLoad</code>, <code>JSONLoad</code>,
        <code>CompoundHash</code>) cover Rails strong parameters/mass
        assignment -- confirmed by reading RuboCop's own Security cops
        documentation page directly before building this.</p>
        <p><b>100% static text analysis</b> -- a regex-based line
        scanner, not a real Ruby parser, so it works whether the Ruby
        plugin is installed or not.</p>
        <p>Stated honestly: v0.1 doesn't resolve whether the receiver
        is actually an <code>ActionController::Parameters</code>
        instance, so an unrelated custom <code>.permit!</code> method
        on some other object is a possible (rare) false positive.</p>
        <p>Free. No license nags, no telemetry, no network calls.</p>
    ]]></description>

    <depends>com.intellij.modules.platform</depends>

    <extensions defaultExtensionNs="com.intellij">
        <localInspection language=""
            groupName="Rails Mass Assignment Companion"
            displayName="permit! allows unrestricted mass assignment"
            enabledByDefault="true"
            level="WARNING"
            implementationClass="dev.gaphunter.railsmassassignmentcompanion.inspection.PermitBangInspection"/>
        <notificationGroup id="Rails Mass Assignment Companion" displayType="BALLOON"/>
    </extensions>
</idea-plugin>
